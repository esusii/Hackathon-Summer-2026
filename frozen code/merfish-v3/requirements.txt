# Python 3.11+ ; a CUDA GPU is strongly recommended (CPU works but is far slower)
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
anndata>=0.10
h5py>=3.10
torch>=2.2          # install the CUDA build: https://pytorch.org/get-started/locally/
xgboost>=2.0        # only needed for the screen_xgb.py baseline comparison
